<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Microphone: required to capture audio playing through the phone speaker -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Foreground service: required since Android 9 (API 28) for reliable
         background microphone access. The OS enforces a visible persistent
         notification while the foreground service is running — this is not
         optional and is the expected UX. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Post notifications: required since Android 13 (API 33) to show the
         foreground service notification and result notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Overlay window: required for Pop-Up Shazam style floating button over other apps -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Floating widget foreground service (specialUse type; Android 14+).
         The type itself needs no dedicated permission — it exists for apps
         whose foreground service purpose has no standard category (here:
         keeping a draggable fact-check bubble over other apps). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- Foreground service for background microphone recording.
           android:foregroundServiceType="microphone" is required since Android 10
           for apps that record audio while backgrounded.
           The persistent notification is mandatory OS behavior: the user sees
           "Aletheia is listening" while recording is active. -->
      <service
        android:name=".AudioRecorderService"
        android:foregroundServiceType="microphone"
        android:exported="false" />

      <!-- Floating widget service: bubble + WebView verdict card over any
           app. Declared specialUse so Android 14+ allows the persistent
           foreground service; the subtype is what Play review asks for. -->
      <service
        android:name=".FloatingWidgetService"
        android:foregroundServiceType="specialUse"
        android:exported="false">
        <property
          android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
          android:value="floating fact-check widget shown over other apps" />
      </service>
    </application>
</manifest>
